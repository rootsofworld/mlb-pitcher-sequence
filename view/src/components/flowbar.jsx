import React from "react";
import * as d3 from 'd3';

function FlowBar(
    typeset=[]
){
    const [selected, setSelected] = React.useState(null)
    const el = React.useRef(null)

    React.useEffect(() => {
        //setting
    })

    return (
        <svg ref={el} width={} height={}/>
    )
}

export default FlowBar;