import React, {useState, useEffect} from 'react';
import * as d3 from 'd3';

/**
 * 
 * @param {Object} props
 * @param {Array<PlateAppearance>} props.data
 */
function PitchFlow(props){
    const [flow, setFlow] = useState()

    return <svg />
}

export default PitchFlow;